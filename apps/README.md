# Aplicações

- `server`: backend Python/FastAPI que roda no Termux;
- `web`: SPA estática compilada e embutida no backend;
- `companion`: APK Android opcional e experimental, sem privilégios.

O código ativo fica em `server`/`web`; o companion exige milestone e ADR
próprios e não foi implementado.
